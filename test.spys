jmp			start

go:
	push	rbp
	mov		rbp, rsp
	sub		rsp, 2

	mov		[rbp - 1], rdx
	add		[rbp - 1], rex
	mul		[rbp - 1], 2
	mov		rax, [rbp - 1]

	mov		rsp, rbp
	pop		rbp
	ret

start:
	mov		rdx, 160
	mov		rex, 2
	call	go	
	null

