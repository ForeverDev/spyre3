call start 
null
null
null

anotherThing:
	push	rbp
	mov		rbp, rsp

	mov		rax, 500

	mov		rsp, rbp
	pop		rbp
	ret

thing:
	push	rbp
	mov		rbp, rsp

	sub		rax, 20
	
	mov		rsp, rbp
	pop		rbp
	ret

start:
	call	anotherThing
	call	thing
	sub		rax, 20
	mov		rbx, 10
	mov		rcx, 30
	ret
